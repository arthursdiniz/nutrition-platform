import { useState } from 'react'; import { Alert, Button, StyleSheet, Text, TextInput, View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { api } from '@/services/api/client'; import { theme } from '@/theme/tokens';
export default function Checkin(){const [water,setWater]=useState('');const [energy,setEnergy]=useState('3');const [saving,setSaving]=useState(false);const save=async()=>{setSaving(true);try{await api.http.post('/progress/checkins',{waterMl:Number(water)||0,energyLevel:Number(energy),hungerLevel:3,sleepQuality:3,mood:3,mealPlanAdherence:3,exerciseMinutes:0});Alert.alert('Check-in salvo','Obrigada por registrar como foi seu dia.');}catch{Alert.alert('Não foi possível salvar','Tente novamente.');}finally{setSaving(false)}};return <SafeAreaView style={s.page}><View style={s.content}><Text style={s.title}>Check-in de hoje</Text><Text style={s.label}>Água em ml</Text><TextInput keyboardType="numeric" value={water} onChangeText={setWater} placeholder="Ex.: 1500" style={s.input}/><Text style={s.label}>Energia de 1 a 5</Text><TextInput keyboardType="numeric" value={energy} onChangeText={setEnergy} style={s.input}/><Button title={saving?'Salvando...':'Salvar check-in'} disabled={saving} onPress={save}/></View></SafeAreaView>};const s=StyleSheet.create({page:{flex:1,backgroundColor:theme.colors.background},content:{flex:1,justifyContent:'center',padding:theme.spacing.xl,gap:theme.spacing.sm},title:{fontSize:28,fontWeight:'700',color:theme.colors.textPrimary},label:{fontWeight:'600',color:theme.colors.textPrimary},input:{backgroundColor:theme.colors.surface,padding:theme.spacing.md,borderRadius:theme.radius.md}});
